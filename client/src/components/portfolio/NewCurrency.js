import React from 'react';
import { Button, Container } from 'semantic-ui-react';

const NewCurrency = () => {
  return (
    <Button content='Add Currency' icon='plus circle' labelPosition='left' />
  );
};

export default NewCurrency;
